Debugging
---------

In order to use the debugger, please ensure that you launch the emulator from the IDE with the additional command ``-debugPort`` followed by the port
you wish to be debugging on. Nesbox uses TCP debugging with the DAP protocol over local host.